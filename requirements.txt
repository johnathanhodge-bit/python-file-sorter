# Requirements for Organizer.py
# 
# This script uses only Python standard library modules:
# - os, shutil, json, logging (all standard library)
# - tkinter (usually included with Python, but may need to be installed on some Linux systems)
#
# If tkinter is not available, install it with:
#   - Ubuntu/Debian: sudo apt-get install python3-tk
#   - Fedora: sudo dnf install python3-tkinter
#   - macOS/Windows: Usually pre-installed with Python

# No additional pip packages are required for this script.

